# 基础工具类
accelerate
codetiming
datasets
dill
hydra-core
liger-kernel
packaging>=20.0
pre-commit
pylatexenc
pybind11
tqdm>=4.66.0

# 核心计算框架（修复版本冲突）
numpy>=1.24.0,<3.0.0          # 统一版本范围
torch>=2.0.0                   # 建议锁定 2.1.x 以兼容 vllm（若启用）
torchdata

# 模型训练相关（统一版本下限）
transformers>=4.30.0
peft>=0.5.0
deepspeed>=0.10.0
tensordict>=0.8.0,<=0.10.0,!=0.9.0  # 保留原版本限制
safetensors
sentencepiece

# 数据处理（统一版本）
pandas>=2.0.0
pyarrow>=19.0.0
scikit-learn>=1.3.0
ijson

# 文本/数学处理（修复命名冲突）
googletrans==4.0.0rc1
latex2sympy2_extended
math-verify                    # 修复：下划线改为短横线（PyPI标准名称）
pylatexenc

# 可视化/日志
wandb
tensorboard>=2.15.0
matplotlib>=3.7.0

# 分布式/并行计算（潜在冲突需注意）
ray[default]

# Web服务/通信
uvicorn
fastapi
bottle
tornado
pyzmq>=25.0.0
pyyaml>=6.0
requests==2.32.5

# PDF处理
grobid-client==0.8.8
grobid-client-python==0.1.2
pymupdf==1.26.7

# 可选依赖（需手动确认版本兼容）
# vllm==0.8.4  # 启用前需确保 torch 版本为 2.1.x
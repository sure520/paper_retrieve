# ============================================
# CPU 版本依赖文件
# 适用于无 GPU 或低显存设备（如 MX250 2GB）
# ============================================

# ============================================
# 1. 基础工具类
# ============================================
accelerate
codetiming
datasets
dill
hydra-core
packaging>=20.0
pre-commit
pylatexenc
pybind11
tqdm>=4.66.0

# ============================================
# 2. 核心计算框架
# ============================================
numpy>=1.24.0,<3.0.0

# ============================================
# 3. PyTorch CPU 版本（必须从特定源安装）
# 安装命令: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# ============================================
torch>=2.0.0
torchdata
torchvision
torchaudio

# ============================================
# 4. 模型训练相关（CPU 友好版本）
# ============================================
transformers>=4.30.0
peft>=0.5.0
# 注意：DeepSpeed 在 Windows CPU 上支持有限，如需使用请参考官方文档
# deepspeed>=0.10.0  # Windows CPU 上可能编译失败，默认禁用
tensordict>=0.8.0,<=0.10.0,!=0.9.0
safetensors
sentencepiece

# ============================================
# 5. 数据处理
# ============================================
pandas>=2.0.0
pyarrow>=19.0.0
scikit-learn>=1.3.0
ijson

# ============================================
# 6. 文本/数学处理
# ============================================
googletrans==4.0.0rc1
latex2sympy2_extended
math-verify
pylatexenc

# ============================================
# 7. 可视化/日志
# ============================================
wandb
tensorboard>=2.15.0
matplotlib>=3.7.0

# ============================================
# 8. 分布式/并行计算
# ============================================
ray[default]

# ============================================
# 9. Web服务/通信
# ============================================
uvicorn
fastapi
bottle
tornado
pyzmq>=25.0.0
pyyaml>=6.0
requests==2.32.5

# ============================================
# 10. PDF处理
# ============================================
# 注意：grobid-client 与 googletrans 存在 httpx 版本冲突
# 如需使用 grobid-client，请注释掉 googletrans 或升级 grobid-client 版本
# grobid-client==0.8.8
grobid-client-python==0.1.2
pymupdf==1.26.7

# ============================================
# 11. 可选依赖（CPU 版本通常不需要）
# ============================================
# vllm 需要 GPU 支持，CPU 版本禁用
# vllm==0.8.4

# liger-kernel 主要用于 GPU 加速，CPU 版本可禁用
# liger-kernel

# ============================================
# 模型相关依赖
# ============================================
dashscope

# ============================================
# 其他依赖
# ============================================
dotenv